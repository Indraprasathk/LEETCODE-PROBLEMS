class Solution(object):
    def maxProductDifference(self, nums):
        nums=sorted(nums)
        i=len(nums)
        max=[nums[i-1],nums[i-2]]
        min=[nums[0],nums[1]]
        x=max[0]*max[1]
        y=min[0]*min[1]
        return x-y
